let minimist,now,forecast,help,configure,version;_2dd‍.x([["cli",()=>cli]]);_2dd‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_2dd‍.w("./now",[["now",["now"],function(v){now=v}]]);_2dd‍.w("./forecast",[["forecast",["forecast"],function(v){forecast=v}]]);_2dd‍.w("./help",[["help",["help"],function(v){help=v}]]);_2dd‍.w("./configure",[["configure",["configure"],function(v){configure=v}]]);_2dd‍.w("./version",[["version",["version"],function(v){version=v}]]);






       async function cli(argsArray) {
  const args = minimist(argsArray.slice(2));
  let cmd = args._[0] || 'help';

  if (args.version || args.v) {
    cmd = 'version';
  } 
  
  if (args.help || args.h) {
    cmd = 'help';
  }

  switch (cmd) {
    case 'version':
      version(args);
      break;

    case 'help':
      help(args);
      break;

    case 'now':
      now(args);
      break;

    case 'forecast':
      forecast(args);
      break;

    case 'config':
      configure(args);
      break;

    default:
      console.error(`"${cmd}" is not a valid command!`);
      break;
  }
}